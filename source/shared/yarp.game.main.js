/**

    database structure for game:
    - entities (req inventory)
    - map
        - grid?
    - items
    - storage
        - inventory
    
**/
if(GLOBAL.Yarp.CONSTANTS.CONTEXT === 0) {
    console.log(GLOBAL);
}
